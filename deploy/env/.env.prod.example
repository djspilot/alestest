# VPS productie .env
# Plaats op de VPS op: /srv/alestest/.env
# NOOIT in git committen

# Verplicht: komma-gescheiden API sleutels
API_KEYS=vervang-met-lange-random-sleutel

# Upload limieten
MAX_FILE_SIZE_MB=100
JOB_TTL_SECONDS=3600

# Lite mode: unfold uitgeschakeld op VPS
DISABLE_STAGES=unfold
