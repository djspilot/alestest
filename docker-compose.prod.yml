services:
  api:
    image: ghcr.io/aidoel/alestest-api:latest
    container_name: ales-api
    restart: unless-stopped
    environment:
      API_KEYS: ${API_KEYS}
      UPLOAD_DIR: /srv/alestest/uploads
      DB_PATH: /srv/alestest/data/db/manufacturing_data.db
      MAX_FILE_SIZE_MB: ${MAX_FILE_SIZE_MB:-100}
      JOB_TTL_SECONDS: ${JOB_TTL_SECONDS:-3600}
      FREECAD_RUNTIME_ROOT: /opt/conda/envs/freecad
      FREECAD_AUTO_INSTALL: 0
      DISABLE_STAGES: ${DISABLE_STAGES:-}
    volumes:
      - ales_uploads:/srv/alestest/uploads
      - ales_db:/srv/alestest/data/db
      - ales_runtime:/srv/alestest/runtime
    ports:
      - "127.0.0.1:8000:8000"


volumes:
  ales_uploads:
  ales_db:
  ales_runtime:
